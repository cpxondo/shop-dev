import { AvailableItemPipe } from './available-item.pipe';

describe('AvailablePipePipe', () => {
  it('create an instance', () => {
    const pipe = new AvailableItemPipe();
    expect(pipe).toBeTruthy();
  });
});
